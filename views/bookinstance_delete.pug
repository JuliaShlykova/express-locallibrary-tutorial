extends layout 
block content
  h1 #{title}: #{bookinstance._id}
  
  p #[strong Are you sure you want to delete this book instance?]
  form(method='POST' action='')
    input(type="hidden" name="bookinstanceid" value=bookinstance._id required=true)
    button.btn.btn-primary(type="submit") Delete